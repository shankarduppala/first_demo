# 🚀 Jenkins CI/CD Pipeline with Docker & Docker Hub

This project demonstrates a **real-time CI/CD pipeline** using **Jenkins**, **Docker**, and **Docker Hub** on a **Windows-based Jenkins agent**.

The pipeline automatically:
- Pulls source code from GitHub
- Builds a Docker image
- Authenticates securely with Docker Hub
- Pushes the Docker image to Docker Hub

---

## 🛠️ Tech Stack

- **Jenkins** (Declarative Pipeline)
- **Docker & Docker Hub**
- **GitHub**
- **Windows Server / Windows Agent**
- **Docker Desktop**

---

## 📂 Project Structure

```bash
.
├── Dockerfile
├── Jenkinsfile
└── README.md
